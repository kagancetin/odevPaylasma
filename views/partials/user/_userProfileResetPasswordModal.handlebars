<div class="modal fade" id="resPassUser{{userInfo._id}}" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
    aria-hidden="true ">
    <div class="modal-dialog" role="document ">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Şifre Sıfırla</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true ">&times;</span>
                </button>
            </div>
            <form class="form-horizontal" method="post" action="/users/resetPassword/{{userInfo._id}}">
                <div class="modal-body">
                    <p>{{userInfo.fullName}} kullanıcısının yeni şifresi:<input name="newPassword"
                            id="newPassword{{data._id}}"></p>
                    <p>Onaylıyor musunuz?</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-primary" data-dismiss="modal" aria-label="Close">
                        İptal
                    </button>
                    <button type="Submit" class="btn btn-primary">Onayla</button>
                </div>
            </form>
        </div>
    </div>
</div>